 <header class="special-menu">
    <div class="special-menu__items special-menu__items_mobile">
        <a href="#">{{ 'plus-icon.svg' | asset_url | img_tag: 'plus' }}</a>
    </div>
    <div class="special-menu__items special-menu__items_desktop">
        <a href="#" class="special-menu__item">shop</a>
        <a href="#" class="special-menu__item">diary</a>
    </div>
    <a href="#" class="special-menu__logo">{{ 'logo.svg' | asset_url | img_tag: 'logo' }}</a>
    <div class="special-menu__items special-menu__items_mobile">
        <a href="#">{{ 'bag-icon.svg' | asset_url | img_tag: 'bag' }}</a>
    </div>
    <div class="special-menu__items special-menu__items_desktop">
        <a href="#" class="special-menu__item">wishlist: <span>75</span></a>
        <a href="{{ routes.cart_url }}" class="special-menu__item">bag: <span>2</span></a>
    </div>
</header>

{% schema %}
{
  "name": "header",
  "tag": "section",
  "class": "section"
}
{% endschema %}
